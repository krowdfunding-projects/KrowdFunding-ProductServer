server:
  port: 0
spring:
  application:
    name: productService
  jpa:
    hibernate:
      ddl-auto: update #?? ????
    properties:
      hibernate:
        format_sql: true
        show_sql: true

  # TODO database ???? ?? ? profile ??
#  datasource:
#    url: jdbc:h2:tcp://localhost/~/
#    username: sa
#    password:


eureka:
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}} #MSA ??? ??
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8888/eureka